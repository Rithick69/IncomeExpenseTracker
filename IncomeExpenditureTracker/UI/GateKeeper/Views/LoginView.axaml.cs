using Avalonia.Controls;

namespace IncomeExpenditureTracker.UI.Gatekeeper;

// UserControl: This is a reusable, nestable block of UI that lives inside an existing window.
public partial class LoginView : UserControl
{
    public LoginView()
    {
        InitializeComponent();
    }
}