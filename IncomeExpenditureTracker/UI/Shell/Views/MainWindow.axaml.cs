using Avalonia.Controls;

namespace IncomeExpenditureTracker.UI.Shell;

// Window: This instructs the operating system to create a brand new physical window with its own title bar, minimize/maximize buttons, and taskbar icon.
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}